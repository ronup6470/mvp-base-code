/**
 * Holds Valid API's for App.
 */
export enum APIPath {
    /** Holds path for gridsizes API. */
    Grisizes = 'gridsizes',

    /** Holds API path responsible to get all sites info. */
    Site = 'sites',
}

