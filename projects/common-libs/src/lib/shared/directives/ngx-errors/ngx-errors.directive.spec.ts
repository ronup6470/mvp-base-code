import { NgxErrorsDirective } from './ngx-errors.directive';

describe('NgxErrorsDirective', () => {
  it('should create an instance', () => {
    const directive: NgxErrorsDirective = new NgxErrorsDirective(null);
    expect(directive).toBeTruthy();
  });
});
