import { SortingOrderDirective } from './sorting-order.directive';

describe('OrderDirective', () => {
  it('should create an instance', () => {
    // const directive = new SortingOrderDirective();
    // expect(directive).toBeTruthy();
  });
});
