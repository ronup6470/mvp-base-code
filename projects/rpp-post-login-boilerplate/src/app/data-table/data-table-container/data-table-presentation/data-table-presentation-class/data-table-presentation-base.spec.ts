import { DataTablePresentationBase } from './data-table-presentation-base';

describe('DataTablePresentationBase', () => {
  it('should create an instance', () => {
    expect(new DataTablePresentationBase()).toBeTruthy();
  });
});
